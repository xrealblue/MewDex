[profile.default]
src = "src"
out = "out"
libs = ["lib"]
optimizer = true
optimizer_runs = 200
viaIR = true

# Use the latest solc version for our contracts
solc = "0.8.20"

# Solidity version overrides for dependencies
# Note: These are for Uniswap V2 contracts which use older Solidity versions
